<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %> fade-in">
    <a href="#" data-dismiss="alert" class="close">×</a>
    <ul>
      <div>
        <%= value %>
      </div>
    </ul>
  </div>
<% end %>
